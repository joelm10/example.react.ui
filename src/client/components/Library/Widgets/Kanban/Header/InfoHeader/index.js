const KanbanInfoHeader = () => {
    return (
        <div className="kanban-info-header">
            The card component uses a schema to define its structure and behavior.
            
        </div>
    );
};

export default KanbanInfoHeader;